object nineth {
  def main(args:Array[String]):Unit={
    var a = -8
    if(a>=0 || a%2==0)
      {
        println("number is either non-negative or even")
      }
    else
    {
      println("number is neither non-negative nor even")
    }
  }

}
